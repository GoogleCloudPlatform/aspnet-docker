runtime: aspnetcore
env: flex

runtime_config:
  startup_project: MainProject\MainProject.csproj

# Sets the image to 'nextcanary' so we can test the image with the
# canary build App Engine Flex.
beta_settings:
  image: nextcanary
